import { RoundReviewScreenProps } from "./RoundReviewScreen.type";



export default function RoundReviewScreen(props: RoundReviewScreenProps) {
    const { session_id, round_id, total_arrows } = props.route.params;
    return (
        <>
        </>
    )
}